import 'package:flutter/material.dart';
class PageTheree extends StatelessWidget {
  const PageTheree({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("page 3"),
      ),
    );
  }
}
